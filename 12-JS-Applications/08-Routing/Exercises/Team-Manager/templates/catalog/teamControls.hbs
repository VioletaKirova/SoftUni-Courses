{{#if isAuthor}}
    <a href="#/edit/{{teamId}}" class="btn btn-default">Edit Info</a>
{{else}}
    {{#if isOnTeam}}
        <a href="#/leave/{{teamId}}" class="btn btn-default">Leave Team</a>
    {{else}}
        <a href="#/join/{{teamId}}" class="btn btn-default">Join Team</a>
    {{/if}}
{{/if}}